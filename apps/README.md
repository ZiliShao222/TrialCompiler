# Applications

- `api/` will expose authenticated workflow, document, review, and audit APIs.
- `web/` will contain the document workspace and benchmark dashboard.
- Feishu is treated as an integration surface, while reusable domain logic stays
  in `src/trialcompiler/`.

