# Configuration

Store versioned, non-secret defaults and provider examples here. Real API keys,
passwords, private endpoints, and enterprise identifiers must use environment
variables or ignored `*.local.*` files.

\n