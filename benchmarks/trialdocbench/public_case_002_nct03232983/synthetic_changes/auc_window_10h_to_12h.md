# Synthetic change: extend the PK window from 10 h to 12 h

This is a synthetic benchmark mutation, not a real amendment to NCT03232983.

Change the primary PK observation window from 600 minutes / 10 hours to 720 minutes / 12 hours.
The expected impact set includes the protocol synopsis, schedule of activities, PK sampling text,
SAP PK parameter definitions, NCA rules, registry outcome time frame, and downstream data tables.
The scorer must protect unrelated uses of the number 10 and must report any residual 600-minute or
10-hour statement in an affected location.
