# Optional model-assisted review. Copy to an ignored local file and replace the value.
DASHSCOPE_API_KEY=

# Optional API persistence location. Generated content should remain under outputs/.
TRIALCOMPILER_DB=outputs/api/memory.sqlite3
